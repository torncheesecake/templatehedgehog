# Debug Prompt

Investigate the reported issue from the failing symptom backwards. Reproduce where possible, inspect relevant routes, components, data, and libraries, then make the smallest safe fix. Run targeted tests first, then the broader validation command that matches the risk.
